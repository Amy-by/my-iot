**10 落地执行清单（可随时更新）**

| 序号 | 任务 | 负责人 | 状态 | 交付物/记录 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 1 | 环境准备：JDK/Node/MySQL/Redis/Maven 安装校验 | 你 | 待开始 | 终端截图或命令输出（java -version / node -v / mvn -v） | 确认端口、版本 |
| 2 | 数据库建库 + 执行初版 V1__init_iot_tables.sql | 我起草，你执行 | 已完成（脚本） | `src/sql/iot/init/V1__init_iot_tables.sql` + 执行日志 | 脚本已生成；请在 DB `iot_v1` 以账号 `root` 执行或接入 Flyway |
| 3 | Flyway 或手工执行策略确定 | 已选 Flyway | 待开始 | 选择方案并记录 | 走 Flyway，需在 application.yml 配置 |
| 4 | 后端骨架（controller/service/mapper/model/config 占位） | 我 | 待开始 | 可编译、可启动的最小版本 | 本仓库为前端代码，后端需在后端仓库实现；包名对齐 `com.ruoyi.iot` |
| 5 | 前端骨架（views/router/api/store 占位） | 我 | 已完成（占位） | `npm run dev` 可访问占位页 | 已新增 `src/views/iot/*.vue` + `src/api/iot/*` + `src/store/modules/iot.js` 占位；路由待按需挂载 |
| 5a | 挂载 IoT 路由/菜单入口 | 我 | 已完成 | 路由/菜单配置提交 | 在侧边栏添加 /iot 入口，指向占位页 |
| 5b | 提供 Flyway 配置示例 | 我 | 待开始 | application.yml 片段 | 便于后端接入 `V1__init_iot_tables.sql` |
| 6 | Netty/WebSocket 端口与鉴权方案确认 | 你 | 待开始 | 配置约定文档 | 方案：同端口 + Bearer Token 鉴权，Netty 复用 Spring Security（待在后端落地） |
| 7 | 日志导出合规策略（脱敏/限流） | 你 | 待开始 | 规则说明 | 方案：手机号/ICCID 脱敏，单次导出≤1000条且需时间范围（待实现） |
| 8 | 模拟器策略（复用/独立） | 你 | 待开始 | 方案记录 | 方案：复用核心逻辑 + 根目录脚本入口（待实现） |
| 9 | 依赖版本管理（version.properties 使用方式） | 我/你 | 待开始 | 构建读取方案说明 | 方案：使用 Maven properties 读取 version.properties（需在后端仓库配置） |

更新规则：完成一项就把“状态”改为“进行中/已完成”并补充交付物、备注。若任务拆分，可增行。***

